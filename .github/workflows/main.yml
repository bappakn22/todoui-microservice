name: My First Workflow

on: 
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: say Hello
        run: echo "Hello Bappa"

      - name: Show Date
        run: date

      - name: Show CUrrent Directory
        run: pwd

